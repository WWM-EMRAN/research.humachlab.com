# Forms Folder

This folder contains optional PHP endpoints inherited from the original static-site template base.

For purely static hosting such as GitHub Pages, PHP will not run. In that case, connect the contact form in `contact.html` to one of the following instead:

- Google Forms
- Formspree
- Netlify Forms
- Vercel/serverless endpoint
- A custom backend API
